export class TimelapseFetcher {
  async fetchForAttempt(_attemptId: string): Promise<{ sourcePath: string | null }> {
    // Placeholder for Bambu LAN retrieval flow.
    return { sourcePath: null };
  }
}
